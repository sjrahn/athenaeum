# macOS Finder metadata
.DS_Store

# tools (Python) build/venv/cache
tools/.venv/
tools/dist/
tools/build/
tools/**/__pycache__/
tools/**/*.egg-info/
tools/.pytest_cache/
tools/.ruff_cache/

# scratch test instance (testdata/) — seeded from a private source instance by
# testdata/seed.py; holds real (if tiny) private artifact bytes, never tracked
/testdata/instance/

# members-parity fixtures — frozen slices of real instance records (third-party
# correspondence, contacts, host configs): instance data, never tracked here.
# The battery self-skips without them; restore a local copy to run it in full.
/tools/tests/data/members_parity/

research

__pycache__/
