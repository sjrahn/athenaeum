"""The codex package — targeting, note generation, build (`spec/codex.md`).

A codex is a view with a voice: scope materialization and the build are
deterministic tooling (`ath codex …`); the editorial voice lives in the
codex's templates. There is deliberately no bare `codex` command.
"""
